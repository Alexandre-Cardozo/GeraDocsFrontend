Dashed upload drop-zone used for DFD, ATA and PCA attachments; shows a removable file chip when filled.

```jsx
<FileUpload file={dfd} onChange={setDfd} placeholder="Clique para selecionar o DFD ou arraste o arquivo aqui" accept=".pdf,.docx,.doc" />
```
