/** White form-section card with display-font title, hint line and soft divider. */
export interface SectionBlockProps {
  title: string;
  /** Methodological guidance sentence under the title. */
  hint?: string;
  children?: React.ReactNode;
}
