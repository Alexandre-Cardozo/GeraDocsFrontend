Pill toggle switch used for optional process phases and settings rows.

```jsx
<Toggle checked={on} onChange={setOn} />
<Toggle checked={on} onChange={setOn} color="#7C3AED" /> {/* fase opcional destacada (violeta) */}
```
