The 240px navy app sidebar — brand chip + wordmark, órgão switcher, "Principal"/"Sistema" nav groups, user row. Active item = royal tint + electric left bar.

```jsx
<Sidebar currentView="dashboard" navigate={setView} />
```

Nav keys map to built-in icons: dashboard, processes, approvals, documents, settings.
