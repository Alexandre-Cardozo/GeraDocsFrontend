Process status pill with dot; the six statuses and their colors are fixed vocabulary — never invent new ones.

```jsx
<StatusBadge status="em_revisao" size="sm" />
```

Statuses: rascunho, em_revisao, aguardando, aprovado, rejeitado, concluido.
